package com.cleardu.app

import android.app.Application

/**
 * Application entry point.
 *
 * Kept minimal for the onboarding prototype. Real app modules
 * (initializers, dependency injection, strict mode, etc.) should be wired
 * here as the project grows.
 */
class ClearDuApplication : Application()
